[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_in_loop"
version = "0.1.0"
description = "AI-in-the-Loop course starter"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["ai_in_loop*"]
